services:
  - type: web
    name: poe-vectara-connector
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port 10000
    envVars:
      - key: VECTARA_CUSTOMER_ID
        sync: false
      - key: VECTARA_API_KEY
        sync: false
